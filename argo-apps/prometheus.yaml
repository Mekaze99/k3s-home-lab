apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: monitoring 
spec:
  project: default
  source:
    repoURL: https://github.com/Mekaze99/k3s-home-lab.git 
    targetRevision: HEAD
    path: platform/prometheus-test
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: apps 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
